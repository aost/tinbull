require 'spec_helper'

describe PostsController do
  describe "POST create" do
    context "with valid attributes" do
      it "creates a new post in topic"
      it "logs ip in post"
      it "redirects to topic"
    end
  end
end
